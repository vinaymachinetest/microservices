spring.application.name=limits-service

#Uset to read from application properties if we configure in spring cloud config server then
#here it is not needed
#limits-service.max=10
#limits-service.min=5

spring.cloud.cofig.uri=http://localhost:8888
spring.profiles.active=dev
